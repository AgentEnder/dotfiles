# Ignores the below files **everywhere**.

# Allows placing .bashrc inside folders to source when entering the folder.
.bashrc
.zshrc
.zed
.superset

**/.claude/settings.local.json

**/.claude/.cc-writes/
